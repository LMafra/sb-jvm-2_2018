
/**
 * @brief printa um inteiro de uma variavel local
 *  
 */
void print_jvm_wide_iload();

/**
 * @brief printa um long int de uma variavel local
 * 
 */

void print_jvm_wide_lload();
/**
 * @brief printa um float de uma variavel local
 * 
 */
void print_jvm_wide_fload();

/**
 * @brief printa um double de uma variavel local
 * 
 */
void print_jvm_wide_dload();

/**
 * @brief printa um ponteiro de uma variavel local
 * 
 */
void print_jvm_wide_aload();

/**
 * @brief printa um int numa variavel local
 * 
 */
void print_jvm_wide_istore();

/**
 * @brief printa um long numa variavel local
 * 
 */
void print_jvm_wide_lstore();

/**
 * @brief printa um float numa variavel local
 * 
 */
void print_jvm_wide_fstore();

/**
 * @brief printa um double numa variavel local
 * 
 */
void print_jvm_wide_dstore();

/**
 * @brief printa um ponteiro numa variavel local
 * 
 */
void print_jvm_wide_astore();

/**
 * @brief printa o retorno de uma subrotina
 * 
 */
void print_jvm_wide_ret();